{
  "name": "task-manager",
  "version": "1.0.0",
  "description": "开发任务管理系统 - 一个基于Node.js和React的全栈任务管理应用",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "start": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "build": "cd frontend && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "task-management",
    "react",
    "nodejs",
    "express",
    "sqlite",
    "antd"
  ],
  "author": "SirAlex10086",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.10.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}